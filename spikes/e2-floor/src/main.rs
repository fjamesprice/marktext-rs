//! E2 build 0 — the floor. `fn main() {}`, nothing else. This is the
//! subtrahend: every other E2 build's delta is (that build's stripped `dist`
//! size) - (this build's stripped `dist` size).
fn main() {}
