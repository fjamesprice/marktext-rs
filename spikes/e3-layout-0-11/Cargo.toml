[package]
name = "e3-layout-0-11"
edition.workspace = true
version.workspace = true
publish.workspace = true
license.workspace = true

[dependencies]
# The released version, as the spot-check arm. E3's numbers of record come from
# `e3-layout-main`; this exists to answer one question for D2 — does tracking
# `main` cost or buy performance — and in particular whether PR #697's CSS
# line-box sizing for mixed inline content, which E1 found moves every vertical
# metric, also moves the clock.
parley = "=0.11.0"

mt-md.workspace = true
mt-doc.workspace = true
